# Tier 1 case (09 §17.3, F7). Synthetic. Correct prose, 하십시오체. 체언 + XSV (고쳤, 통과했) and a comma after -고.
id: t1-correct-01-prose
profile: agent-reply
arm: any
layer: policy
input: "설정 파일을 읽는 함수를 고쳤습니다. 이제 캐시가 비어 있으면 파일을 다시 불러옵니다. 테스트는 모두 통과했고, 응답 시간도 조금 줄었습니다."
expect:
  noun_ending_ratio.ratio:
    max: 0
  particle_absence_ratio.ratio:
    max: 0.5
